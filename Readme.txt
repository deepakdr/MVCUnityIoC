This program, uses a service which is required by ExternalServiceController which is injected through in the constructor.

Files involved to achieve the Unity IoC
- Global.asax
- Bootstrapper.cs
- Web.config